#!/usr/bin/env bash
##extract data from mysql

mysql -h {{ var.value.host }} -P 3306 -u {{ var.value.user }} -p{{ var.value.password }} -D {{ var.value.db }} -e "select * from account;" > {{ var.value.path }}/account.tsv && mysql -h {{ var.value.host }} -P 3306 -u {{ var.value.user }} -p{{ var.value.password }} -D {{ var.value.db }} -e "select * from card;" > {{ var.value.path }}/card.tsv && mysql -h {{ var.value.host }} -P 3306 -u {{ var.value.user }} -p{{ var.value.password }} -D {{ var.value.db }} -e "select * from client;" > {{ var.value.path }}/client.tsv &&mysql -h {{ var.value.host }} -P 3306 -u {{ var.value.user }} -p{{ var.value.password }} -D {{ var.value.db }} -e "select * from disp;" > {{ var.value.path }}/disp.tsv && mysql -h {{ var.value.host }} -P 3306 -u {{ var.value.user }} -p{{ var.value.password }} -D {{ var.value.db }} -e "select * from district;" > {{ var.value.path }}/district.tsv && mysql -h {{ var.value.host }} -P 3306 -u {{ var.value.user }} -p{{ var.value.password }} -D {{ var.value.db }} -e "select * from loan;" > {{ var.value.path }}/loan.tsv && mysql -h {{ var.value.host }} -P 3306 -u {{ var.value.user }} -p{{ var.value.password }} -D {{ var.value.db }} -e "select * from trans;" > {{ var.value.path }}/trans.tsv